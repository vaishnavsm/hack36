- args:
    permission:
      columns:
      - latitude
      - longitude
      - types
      filter:
        hasura_id:
          $eq: X-HASURA-USER-ID
    role: user
    table: profile
  type: create_update_permission
